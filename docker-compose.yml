services:
  # db:
  #   image: postgres:latest
  #   container_name: analytics_db
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: AnlytcDatabase
  #   ports:
  #     - "5433:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

  api:
    build:
      context: .
      dockerfile: WebAPI/Dockerfile
    container_name: analytics_api
    # depends_on:
    #   - db
    ports:
      - "5126:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=host.docker.internal;Port=5432;Database=AnlytcDatabase;Username=analytics_user;Password=tlG9acxJPOMpU56RLx
      - Jwt__SecretKey=YourSuperSecretKeyThatIsAtLeast32CharactersLongForAnalyticsBE!
      - Jwt__Issuer=Analytics_BE
      - Jwt__Audience=Analytics_BE_Users

# volumes:
#   pgdata:
